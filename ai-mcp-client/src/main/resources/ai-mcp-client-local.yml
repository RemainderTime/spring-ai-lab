spring:
  ai:
    mcp:
      client:
        stdio:
          servers-configuration: classpath:/config/mcp-servers.json
    zhipuai:
      api-key:
      chat:
        options:
          model: glm-4.6
          temperature: 0.7